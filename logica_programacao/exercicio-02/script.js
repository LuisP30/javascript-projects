/* Escreva uma função chamada ePaisagem que
recebe dois argumentos, largura e altura
de uma imagem (number). Retorne true se a imagem
estiver em modo paisagem */

const ePaisagem = (larg, alt) => larg>alt
console.log(ePaisagem(1080, 960))